import { PreventEnterDirective } from './prevent-enter.directive';

describe('PreventEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventEnterDirective();
    expect(directive).toBeTruthy();
  });
});
